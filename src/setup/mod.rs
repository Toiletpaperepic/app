// use std::process;
// use which::which;

pub(crate) fn gotosetup() {
    // let result = which("rustc").unwrap();
    // println!("{}", result.display());
    // process::exit(0);
    unimplemented!()
}